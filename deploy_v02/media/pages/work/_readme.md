# Work page — no media of its own

The Work page just lists every project from `proto/data.jsx` — it reuses each
project's `hero` image/video from `media/projects/<slug>/`. Nothing to upload
here; add/update media in the project's own folder instead.
